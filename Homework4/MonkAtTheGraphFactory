#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;

    int sum = 0, num;

    for(int i = 1; i <= n; i++){
        cin >> num;
        sum += num;
    }

    if (sum / 2 == n - 1)
        cout << "Yes";
    else cout << "No";
    
    return 0;
}
